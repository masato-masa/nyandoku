# にゃんどく（猫の視野パズル）

meowdoku 風の UI を持つ、Akari（美術館）系の論理パズル。Vite + React 19 + TypeScript。

## 最優先の運用ルール

**修正したら必ず `npm run deploy` で GitHub Pages に反映する。** ユーザーは Pages 上で
確認するので、ローカルで直しただけでは「直っていない」ことになる。

```bash
npm run build && npm run deploy
```

`gh-pages` ブランチへ orphan commit を 1 つ push する仕組み（`scripts/deploy.mjs`）。
GitHub Actions は使っていない（トークンに `workflow` スコープが無いため）。

## ゲームのルール

- 盤面は正方形のマス目。一部が**壁**で、壁の一部に**数字**が書いてある。
- 数字は、その壁の**周囲 8 マスにいる猫の数**を表す。
- 猫を置けるのは、**数字付きの壁の周囲 8 マス**だけ（候補マス）。
- 猫の**視野** = 自分のマス + 周囲 8 マス + 上下左右への直線（壁で止まる）。
- **壁でない全マスが誰かの視野に入れば**クリア。
- 猫は同じ行・列に並んでよい。**猫は壁と同じく視線を遮る**。
- マークは 3 状態: なし(0) / ×(1) / 猫(2)。

## ファイルの地図

| ファイル | 役割 |
|---|---|
| `src/core/puzzle.ts` | 生成・解答器・難易度採点。**最重要かつ最大**（約 1000 行） |
| `src/core/game.ts` | 盤面の状態機械。`tap` / `paint` / `undo` / `reset` / `hint` |
| `src/core/puzzleWorker.ts` | 生成を別スレッドで走らせる Worker の入口 |
| `src/core/puzzleClient.ts` | 生成の窓口。キャッシュ・先読み・Worker 失敗時の同期フォールバック |
| `src/core/puzzleStore.ts` | 作った盤面を localStorage に残す（135 バイト/面） |
| `src/App.tsx` | 画面全体。ローディング・ヘッダ・ヘルプ・レベル一覧・クリア演出 |
| `src/ui/Board.tsx` | 盤面。ドラッグ塗り・タップ判定 |
| `src/ui/Cat.tsx` | 猫の画像表示。まばたきタイマーと表情 |
| `src/ui/styles.css` | 参考画面から実測した色・角丸・余白 |
| `scripts/audit-levels.mjs` | **常設の監査ツール。重みを変えたら必ず走らせる** |
| `scripts/slice-cats.mjs` | 素材画像を 4 表情に切り出す |

## 生成の仕組み

**決定的**。レベル番号から `mulberry32` で乱数を種付けするので、同じレベルからは
必ず同じ盤面が出る。だから作り直しは不要で、キャッシュと保存が成立する。

### 4 段階のフォールバック梯子（`generateForLevel`）

| 段階 | 条件 |
|---|---|
| 1 | 通常（許容 13%） |
| 2 | 許容 0.4 / つまみを易しい側へ 0.5 |
| 3 | **難易度を問わない**（許容 ∞ / つまみ 1.0）。ここでほぼ必ず成功する |
| 4 | 最後の逃げ道。**100 レベル前**の盤面を再利用（直前だと使い回し感が出るため） |

予算を 1 まで潰しても 12/12 成功・例外 0 を確認済み。

### 探索の予算

```
KNOB_ATTEMPTS       44     つまみを引く回数
BOARD_ATTEMPTS      2200   1 つまみあたりの盤面試行
TOTAL_BOARD_ATTEMPTS 20000 全体の上限
CLOSE_ENOUGH        0.13   目標にこれだけ近づいたら早期終了
SOLVER_NODE_BUDGET  4000   解の一意性判定の探索上限（超えたら盤面ごと捨てる）
```

**「44」は 44 個の候補から選んでいる、という意味。** 早期終了があるので実際には
44 個引き切らないことも多い（低レベルは平均 14 個程度）。

## 解法の分類

人間が実際に使う順に並べた分類。難易度採点はこれに基づく。

| 記号 | 名前 | 内容 |
|---|---|---|
| A | 数字の充足 | 数字ちょうどになったら残りは×、残り枠＝残り候補なら全部猫 |
| B | 被覆の一意 | 「このマスを見られるのはここだけ」→ そこに猫 |
| C | 集合の推論 | 「どれかは分からないがこの中に猫がいる」を別の数字に流用する |
| E | 背理法 | 仮に置いて矛盾を出す。**深さ 4 まで。二重背理法は無し** |

C の実装は、数字の壁（min=max=数）と被覆集合（min=1, max=候補数）を集合として
持ち、2 つの集合の重なりから外側の下限・上限を出す方式。

## 難易度の採点

```
難易度 = √候補マス数 × 2.5          盤面の広さ
       + 連鎖段数 × 2                確定の連鎖が何段続くか
       + 被覆コスト                  初出 12 + 反復 1.5 × (回数-1)
       + 集合コスト                  初出 22 + 反復 2   × (回数-1)
       + 背理法コスト
```

背理法コスト（1 回ぶん）:

```
段数コスト × 幅の倍率
  段数: {1:40, 2:85, 3:145, 4:225}
  幅  : {2:1.0, 3:1.5, 4:2.1, 5:2.8}、6 以上は 3.6
2 回目以降は × 0.35（同じことの繰り返しなので割り引く）
```

### 採点の設計思想（ここが議論の核だった）

1. **初出と反復を分ける。** 同じ解法を 10 回使っても 10 倍難しくはない。難しさは
   「その解法に気づくまで」にある。ただしゼロにもしない（1 回と 10 回は違う）。
2. **広さは平方根。** 線形にすると「盤を大きくすれば難しい」になってしまう。
3. **幅は加算でなく倍率。** 幅と段数は独立でなく掛け合わさって効く。
4. **連鎖段数 = 背理法開始からの確定連鎖の段数。** マス数ではない。
5. **絶対に「条件」で縛らない。** 「レベル N 以上は背理法必須」のような
   ハードな条件は入れない。難易度はあくまで「なんとなく徐々に難しくなってきた」
   体験のためのもの。**出させたい解法があるなら重みと目標値を上げる。**

### レベル → 目標難易度

```ts
base = 15 + 190 * (1 - exp(-(level - 1) / 60))   // 205 に漸近
target = base * (1 ± 0.2)                        // ゆらぎ
```

レベル 15 が 55 前後になるよう立ち上がりを調整してある（実際に遊んで
「レベル 15 くらいがちょうどいい」という感触を基準にした）。

つまみ（盤サイズ・壁密度・数字割合）はレベルで確率分布が変わるだけで、
**小さい盤も高レベルで出続ける**。サイズは難易度の一要素でしかない。

## 実測で分かっていること（再測定の手間を省くため）

- **背理法つきの盤面は有効な盤面全体の 1〜2% しかない。** 数字割合を 0.55〜0.95 で
  振っても変わらない（0.4〜1.6%）。壁密度でも大きくは変わらない。
- **にもかかわらず高難易度帯では 38% が背理法つきになる。** 高レベルの目標(205漸近)は
  背理法なしの上限(136)では届かないため早期終了せず 44 個を引き切り、
  「目標に一番近い＝一番難しい＝背理法つき」が自動的に選ばれるから。
  44 個中平均 0.75 個しか無くても、**あれば必ず選ばれる**のでこうなる。
- 生成時間は**中央 2.1 秒**まで伸びた（目標に届かず毎回 44 個引き切るため）。
  先読み（プレイ中に次の面を作る）と localStorage 保存で見える待ちはほぼ無いが、
  これは難易度の漸近値を 205 に上げた代償である。下げれば早期終了が復活して速くなるが、
  背理法の出現率は落ちる。**次に調整するならこのトレードオフが論点。**
- 低密度は高コスト: 8×8 @密度0.24 は 1 面 20 秒。6×6 なら 55ms 以下。
- 保存は 135 バイト/面、600 面で 79KB（上限 600 面、あふれたら低いレベルから捨てる）。

## 踏んだ罠（同じ失敗を繰り返さないために）

- **領域成長型の生成は n≥5 で解が一意にならず 0%。** 「蛇状」に一方向へ 3〜8 マス
  伸ばす成長に変えて解決。
- **盤面がタップでずれた。** `.board` に `grid-template-columns` はあるが
  `grid-template-rows` が無く、行が内容依存だった。明示的に
  `repeat(n, 1fr)` を入れ、セルに `min-width/height: 0`、`.mark` を
  `position: absolute` にして解決。
- **数字が 0〜2 にしかならなかった。** 「原理的に無理」と誤って断言したが、
  実際は被覆を貪欲に最大化していたせいで猫が散っていただけ。
  「新しく 1 マス以上増える候補からランダムに選ぶ」に変えて解決。
- **バックグラウンドタブでローディングが永久に止まった。** `requestAnimationFrame`
  は非表示タブで発火しない。Worker 化で解消。
- **走査順に依存していた。** 壁の順を逆にすると連鎖段数が 14/25 で変わった。
  推論を**レベル同期（一括適用）**にして 0/25 に。
- **背理法が連鎖段数を二重計上していた。** propagate を跨いで足していたのが原因。
  最大値を取る方式に修正。
- Windows 環境で bash heredoc が壊れやすい。日本語を含むファイル編集は
  Write ツールか Python スクリプト経由が安全。

## 1000 レベル監査の結果（2026-09-11 時点）

`node scripts/audit-levels.mjs 1000` を 41 分かけて完走させた結果。

```
段階      1 通常 1000 / 2,3,4 はゼロ    ← フォールバックは一度も発動していない
健全性    論理で解けない面 0 / 背理法 4 段超 0
難易度    最小 24 / 25% 107 / 中央 116 / 75% 130 / 最大 305
肉球      {1:4, 2:8, 3:6, 4:4, 5:978}   ← 978/1000 が 5。表示が機能していない
生成時間  中央 2092ms / 90% 4585ms / 最悪 13178ms (Lv310)
```

**良い点**: 梯子の 2〜4 段目が一度も要らなかった。解けない面ゼロ、深さ超過ゼロ。

**問題**: 難易度が 107〜130 に密集しているのに、肉球の閾値が 36/50/62/72 のまま。

## 未解決 / 次にやること

1. **肉球の閾値を引き直す。** 上の分布から五分位で切り直すのが素直
   （目安: 100 / 112 / 122 / 135）。ただし表示として意味を持たせたいなら、
   分布そのものを広げる方が本筋かもしれない。
2. **生成時間 2.1 秒をどうするか。** 上のトレードオフを参照。
3. **重みを変えたら必ず 1000 レベル監査を走らせる**（41 分かかるので裏で回す）。
   段階 4 が出ないこと、段階 3 の難易度が異常でないことを確認する。
