# にゃんどく

猫の視野で部屋を埋める論理パズル。美術館（Akari / Light Up）の変種。

- 盤面の一部が**壁**。壁の一部には**数字**が書いてある
- 数字＝その壁の**隣接8マス**にいる猫の数（ちょうど）
- 猫は**数字付きの壁の隣接8マス**にのみ置ける
- 猫の視野＝自分のマス＋隣接8マス＋上下左右の直線（壁で停止）
- **壁以外の全マス**が誰かの視野に入ったらクリア

## 動かす

```bash
npm install
npm run dev      # http://localhost:5173
npm run deploy   # ビルドして GitHub Pages に反映
```

公開版: https://masato-masa.github.io/nyandoku/

`?level=10` のようにクエリを付けると、その面から始められる（動作確認用）。

到達したレベルは `localStorage` の `nyandoku.maxLevel` に保存され、
右上のレベルボタンから 1 〜 最新まで選び直せる（下から上の順に並ぶ）。

## 何をどこに置いているか

```
src/core/puzzle.ts   盤面の生成とソルバ。DOM に触らない純粋ロジック
src/core/game.ts     ゲームの状態遷移。タップ／ドラッグ／取り消し／ヒント
src/core/sfx.ts      効果音。音声ファイルを持たず WebAudio で合成する
src/ui/Board.tsx     盤面の描画と、指の動きの解釈
src/ui/Cat.tsx       猫の画像切り替え（まばたき・表情）
src/ui/icons.tsx     アイコンの SVG
src/ui/styles.css    配色と寸法
src/App.tsx          画面の組み立てと、状態と音の接続
```

中身のパズルを差し替えるときは `core/puzzle.ts` を置き換える。
`ui/` と `sfx.ts` はそのまま使い回せる。

## 猫の素材

`public/cats/` の 4 枚（通常・まばたき・喜び・がっかり）は ChatGPT に描かせた
オリジナル。参考アプリの絵をそのまま持ってきてはいない。

```
assets-src/cats-grid.png   ChatGPT が出した 2x2 グリッド（1254x1254）
  ↓ node scripts/slice-cats.mjs
public/cats/{normal,blink,happy,sad}.png   256x256 / 透過 / 各 25KB 前後
```

グリッドは目分量で並んでいて四等分では位置も大きさも揃わないため、各象限を
アルファの外接矩形で切り直し、4 枚に共通の縮尺をかけてから透明な正方形の
中央に置いている。これをやらないと表情を切り替えた瞬間に猫が伸縮する。

| 場面 | 猫の動き |
|---|---|
| 常時 | 3.6 秒周期のごく浅い呼吸（拡大率 1.035） |
| 考えている間 | 2.6〜6.8 秒の不規則な間隔で 130ms のまばたき。個体ごとに位相をずらす |
| 置いた瞬間 | バネで飛び出す（少し回転しながら） |
| クリア | `happy` に切り替わり、上から順に跳ねる |

## 操作感の作り

- **ドラッグで X を連続塗り** — なぞり始めたマスが空なら塗り、X なら消す。
  指を離すまでその役割は変わらない。
- **経路補間** — 指が速いと `pointermove` が飛ぶので、前回位置から線分を歩いて
  マスを拾う。これが無いと速くなぞったときだけ穴が空く。
- **タップとドラッグの分離は 7px** — 小さいと普通のタップが塗りになり、大きいと
  塗り始めが鈍る。
- **バネアニメーション** — Motion に任せ、イージング時間を手で決めていない。
- **音程が上がる効果音** — WebAudio 合成。音声ファイルを持たない。
- **閉じる途中のダイアログはクリックを通す** — 消えるアニメーションの 0.2 秒間も
  オーバーレイは残っているので、`exit` で `pointer-events` を切っている。
  これが無いと、閉じた直後に押したボタンが無反応になる。
- **レイアウトを絶対に動かさない** — 盤面の行と列を両方 `1fr` で固定し、猫や X は
  `position: absolute` で置く。当初これを怠っていたため、猫を置いた行だけ高さが
  変わり（`139.688px 87.4375px 87.4375px 87.4375px`）、タップのたびに画面全体が
  ずれていた。セルが正方形ですらなかった。

## 見た目の数値の出どころ

meowdoku のブラウザ版を開き、計算済みスタイルから抜き出した実測値。
目分量で似せた数字は入れていない。

| 項目 | 値 |
|---|---|
| 背景 | `#F6F5EF` |
| 盤面 | 白 / 角丸 16px / 内側余白 16px / セル間 6px |
| セル | 角丸 8px |
| 見出し | `Level N` `#735056` 24px / 600 |
| ヘッダー | `1fr auto 1fr` / gap 12px |
| 進捗ピル | 白 / 丸型 / padding 4px 12px 4px 8px / gap 6px / 猫アイコン 24px |
| 進捗の数字 | 18px / 500。現在値 `#22C55E`、総数 `#71717A` |
| 丸ボタン | ヘッダー 32px・フッター 60px、どちらも白 / `0 1px 2px rgb(0 0 0 / .05)` |
| 書体 | Open Sans |

遊びかたは常時表示をやめ、右上の `?` から開くダイアログに入れてある。
ダイアログとレベル一覧は、参考画面のピル（背景 `#FBF1EB` / 文字 `#8A5A5A` /
角丸 12px）をそのまま行の見た目に流用している。

盤面の色だけはこのゲーム独自。見えていない床 `#EBE4DC`、視野に入った床
`#FBD97E`、猫のいる床 `#F7C95C`、壁 `#6F5A5E`。

## 難易度の設計

盤面の大きさをレベルから直接決めるのはやめ、**複数の軸で候補を作り、難易度の点数が
そのレベルの目標に一番近いものを選ぶ**形にしている。おかげで高いレベルでも小さい盤面が
出るし、レベルと難しさは「傾向として」しか結び付かない。

### 軸（`Knobs`）

| つまみ | 何を動かすか |
|---|---|
| `n` 盤面の一辺 | 見る範囲の広さ |
| `wallDensity` 壁の割合 | 下げると視線が伸び、被覆の推論が増える。ただし生成コストが跳ね上がる |
| `numberRatio` 数字を書く壁の割合 | 下げると手がかりが減る。同じく生成が遅くなる |

実測（n=7、論理で解ける問題のみ）:

| 密度 | 数字 | 被覆推論 | 往復 | 1問あたり |
|---|---|---|---|---|
| 0.30 | 0.6 | 4.9 回 | 4.8 | 948ms |
| 0.48 | 0.8 | 1.4 回 | 3.5 | 1ms |

**面白い方向と速い方向が正面から衝突する**ので、大きい盤ほど密度の下限を上げて
折り合いをつけている。

### 難易度の点数（`DIFFICULTY_WEIGHTS`）

```
点数 = 候補マス数 × 0.6 + 被覆推論の回数 × 6 + 推論の往復回数 × 4
```

候補マス数を重くするとサイズがそのまま難易度になり、高いレベルに大きい盤しか
出なくなる。推論の深さを重くすることで、小さい盤でも難しくなりうる形にした。
結果、サイズ別の難易度の幅は次のように重なる。

```
5x5  19〜43     6x6  20〜51     7x7  27〜63     8x8  40〜61
```

### 測定器（`analyse`）

「解が一意か」と「詰まらずに解けるか」は別問題。人が使う推論を機械的に回して、
どこまで確定できるかを測っている。

**基本推論**（まずこれだけで回す）

- **数字の推論** ある壁の数字が k のとき、まわりの猫が k 匹揃えば残りは空。空きマスを
  全部使わないと k に届かないなら残りは全部猫
- **被覆の推論** まだ誰にも見えていないマスを見られる候補が 1 つしかないなら、そこは猫

**背理法**（基本推論で詰まったときだけ降りる）

あるマスを猫だと仮定して基本推論を回し、矛盾したら猫ではない、と確定する 1 マス仮定。
入れ子にはしない（二重の背理法を要求する問題は「解けない」と判定されて捨てられる）。

**探し方を人間に寄せている。** 盤面中を全部試して最も浅い矛盾を採るのではなく、
まず**分岐の幅**（その数字のまわりに未確定マスが何個残っているか）が最も狭いところに
絞り、その中で最も浅い矛盾を採る。人は「まだ 2 マスしか残っていない数字」から
手をつけるのであって、盤面全体を総当たりするわけではないため。

この違いは大きい。全体から最浅を採る作りだと、実測ではどの局面でも 1 段が
見つかってしまい（壁が密なので、どこに仮定しても 1 波で近くの壁が破綻する）、
深さが難易度の軸として機能しなくなっていた。これは理論的な必然ではなく、
今の盤面構成ゆえの結果で、壁を減らせば段数は伸びる。

得点は「段数」を基準にし、二択の状況が無く三択・四択からしか入れない局面には
幅のぶんを加算する。

```
段数 1:12  2:20  3:28  4:36        幅 2:+0  3:+6  4:+12  5以上:+18
連鎖の中の被覆推論 1 回につき +5
```

実測（レベル 1〜45、背理法 28 回）:

```
分岐の幅   2:17回  3:9回  4:2回
段数       1:27回  2:1回
```

**どちらでも解けない問題は出さない。** 導入前は 8x8 で 11/40 が推測を要していた。
また導入前は数字の推論 24.8 回に対し被覆の推論が 1.9 回しかなく、実質
マインスイーパになっていた。

### 表示（`difficultyBand`）

画面にはレベルの下に肉球 5 つで難易度を出している。点数の分布
（レベル 1〜80 で最小 19・最大 95、四分位 51 / 59 / 66 / 74）を見て
32 / 47 / 62 / 77 で区切り、遊んで「ちょうどいい」と感じたレベル 15 が
3 つになるよう合わせた。

### 難易度の曲線

```
目標難易度 = 18 + 55 × (1 - exp(-(レベル-1) / 16))   ±20% の揺らぎ
```

レベル 15 が 50 前後になるよう合わせてある。実際に遊んで「レベル 15 くらいが
ちょうどいい」という感触が得られた点を基準にした。そこから上は背理法つきの問題に
手が届く高さまで伸びる。実測:

```
Lv 1  5x5 難易度19            Lv30  8x8 難易度72 背理法1 往復7
Lv15  8x8 難易度54 被覆3      Lv60  7x7 難易度72 被覆4 往復8
Lv25  8x8 難易度47            Lv80  6x6 難易度69 背理法1 被覆3
```

Lv80 が 6x6 で難易度 69 になるように、**小さい盤でも高難易度が成立する**。

### 生成のコストと待ち時間

生成は難しいレベルほど時間がかかり、実測で 4 秒近い面がある。コストは採用された
盤面ではなく**捨てられた候補側**にある。最終盤面の一意性チェックは 0.1ms しか
かからないのに、当たりを引くまでの試行が積み重なって秒単位になる。

そこで 3 段構えにしている。

1. **別スレッド**（`puzzleWorker.ts`）で生成する。メインスレッドで回すと、
   待っている間ローディングの猫まで止まって固まったようにしか見えない
2. **先読み** 面を開いたら次のレベルを裏で作る。1 面解くのに数分かかるので、
   その間に作り終わっていれば「次のレベルへ」の待ちは消える（実測 0ms）
3. **キャッシュ** 生成は決定的なので、同じレベルを開き直すと 0ms

### 生成が行き詰まったときの逃げ道

条件に合う盤面が見つからないことがある。無限に探し続けて画面が出ないのが最悪の
結果なので、段階的に諦める。



検証として試行予算を 1 まで絞って 2〜4 を強制的に発動させたところ、12 レベル
すべてで有効な盤面が返り、例外は出なかった。段階 4 が働くと連続するレベルが
同じ難易度になるので、そこで発動が分かる。

打ち切りはすべて**時間ではなく回数で**行う（`KNOB_ATTEMPTS` / `BOARD_ATTEMPTS` /
`TOTAL_BOARD_ATTEMPTS` / `SOLVER_NODE_BUDGET`）。実行速度で結果が変わると
「同じレベルは同じ盤面」が崩れるため。

とくに `SOLVER_NODE_BUDGET` は効く。壁と数字が少ない盤面では一意性の探索が
組合せ爆発し、1 回で数秒かかることがある。試行回数の総枠だけでは 1 回あたりの
コストが桁違いに動くので時間を抑えられなかった（総枠 20000 回でも 11 秒）。

### 盤面の作り方でわかったこと

**猫の選び方が、そのまま数字の分布になる。** 当初は「最も多く新しく照らす猫」を
貪欲に選んでいた。これは視野の重複を避けようとするので猫が散らばり、同じ壁の
周りに 2 匹以上が並ばなくなる。結果として数字が 0 と 1 ばかりになっていた。

| 8x8・50盤面 | 0 | 1 | 2 | 3 | 4 | 猫 |
|---|---|---|---|---|---|---|
| 最大利得を選ぶ | 231 | 471 | 155 | 12 | 0 | 7.8 |
| 候補から無作為 | 159 | 436 | 300 | 78 | 6 | 10.6 |

**「猫も視野を遮る」は解に影響しない。** 遮った猫が必ず同じ方向へ視野を出し直す
ので、見えるマスの和集合が遮蔽の有無で一致する。よって計算では遮蔽を無視でき、
各マスの視野を盤面ごとに 1 度だけ求めて使い回せる。

## ライセンス

自前の実装。meowdoku のコードは使っていない（非公開のため使えない）。
参照した配色・寸法は、公開されている Web ページから計測した数値。
